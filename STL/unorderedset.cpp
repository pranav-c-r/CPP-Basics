#include <bits/stdc++.h>
using namespace std;

void explainUSet(){
    unordered_set<int> st;
    // lower_bound and upper_bound function
    // does not work, rest all functions are same as above
    // unique but not sorted
    // doesnt store in any particular order.
    // so it has better complexity than set in most cases.
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    return 0;
}