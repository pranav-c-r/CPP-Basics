#include <bits/stdc++.h>   // includes all the libraries.
                           // we may think it takes a lot of time.
                           // but actually it doesnt take much time. its fine.
using namespace std;  

int main(){
    int x, y;
    cin >> x >> y;
    cout << "Value of x : " << x << " and y : " << y;
    return 0;
}