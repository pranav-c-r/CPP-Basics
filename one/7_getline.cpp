#include <bits/stdc++.h>   
using namespace std;  

int main(){
    // getline

    string str;
    getline(cin,str);
    cout << str;   // prints the entire line (upto the first newline character)
                   // stops at line break
        
    return 0;
}