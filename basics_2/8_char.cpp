#include <bits/stdc++.h>   
using namespace std;  

int main(){
    // char
    char ch = 'H';
    cout << ch;

    char cha;
    cin >> cha;
    cout << cha;

    string chi = "H";
    cout << chi;

    // generally chars we use single quote
    // strings double quote

    return 0;
}